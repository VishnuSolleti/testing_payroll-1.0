services:
  user_management:
    image: ${USER_MANAGEMENT_IMAGE}
    container_name: payroll_service
    ports:
      - "8001:8000"
    command: >
      gunicorn tara_user_management.asgi:application --host 0.0.0.0 --port 8000
#    env_file:
#      - .env

